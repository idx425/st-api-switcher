# Discord 发布文案 · API 快切 v3.7.2

> 下面两段可直接复制 to Discord。第一段是完整介绍帖，第二段是更新公告（可单独发或跟在主帖后面）。

---

## ① 完整介绍帖（推荐置顶 / 首发）

```
════════════════════════════════════
🔌 API 快切 · ST API Switcher  v3.7.2
一键切换 OpenAI 兼容接口的 URL + Key + 模型
════════════════════════════════════

仓库：https://github.com/idx425/st-api-switcher
协议：MIT · 免费 · 开源
兼容：SillyTavern / 酒馆 1.12+（含移动端壳、TauriTavern 等）

────────────────
为什么要做这个
────────────────
酒馆自带的「连接配置文件」不会给每个自定义端点单独存 Keys。
自定义源共用一个 Key 槽位——换站经常要：改 URL → 重贴 Key → 再 Connect。
API 快切把「站点名 + URL + Key + 模型」绑成一条配置，点一下整套切换并自动重连。

────────────────
核心能力
────────────────
• 一键切换：自动切到 OpenAI → Custom，写入 URL / Key / 模型，并触发 Connect
• 🔍 站点搜索：设置面板 / 快捷面板 / 插头嵌入区均可实时搜索过滤（名称 / URL / 模型 / 分组）
• 多站点管理：新增 / 编辑 / 删除，名称防重名，支持覆盖同名
• 同站多模型：一个 URL / Key 可保存多个模型，快捷面板/插头面板可折叠展开并一键切模型
• 分组折叠：站点可归组，组头可收起；当前连接所在组有 LIVE 标记
• 分层分页（每页 4 条）：未分组站点、分组列表、组内站点、表单分组芯片各自分页
• 获取模型：从目标接口拉 /models，弹窗搜索点选
  – 优先浏览器直连（跟手机/本机网络一致）
  – 失败再走酒馆服务端代理
  – 拉模型临时写 Key 后会 await 还原，减少污染当前连接
• 密钥只留一条：对 api_key_custom 串行「先写新密钥 → 再删旧条目 → 同步 secret_state」
  减少连点切换时的「Secret id not found」、密钥堆成十几条
• 原生 API 自动同步：在酒馆原生 API 面板填写 URL / Key / 模型并点 Connect 后，自动合并到 API 快切
• 导入 / 导出：JSON 备份迁移（注意：含明文 Key，只给自己用）
• 一键更新：面板顶部检查更新；启动后静默检测，有新版本可点通知更新

────────────────
三个入口（三端都能用）
────────────────
1）🔌 插头主界面（手机主推）
   打开「API Connections / 插头」抽屉 → 内嵌「API 快切」列表，点站点即切换
   支持搜索过滤，快速定位目标站点

2）🪄 魔法棒快捷面板
   扩展菜单 →「API 快切」→ 浮层站点列表秒切
   内置搜索框，输入即过滤
   切换后面板保持打开，可连续换站
   关闭：点标题 × / 点面板外 / 再点一次菜单项 / Esc
   （× 只关快切，不连带关魔法棒菜单）

3）扩展设置完整面板
   扩展 → API 快切：管理站点、分组、导入导出、获取模型、检查更新
   使用原生 inline-drawer，可收成小横条
   搜索栏可按名称/URL/模型/分组过滤

────────────────
界面与适配
────────────────
• 科幻 HUD 风：暗玻璃 / 扫描感 / 渐变边框（插件自绘区）
• 浅色主题可读：表单岛强制暗底亮字，避免白字撞白底
• 手机 / 平板 / 电脑：
  – 模型选择全屏居中、可滚动、键盘弹出不把弹窗顶飞
  – 魔法棒面板抬到 html 根 + 高 z-index，避让半透明菜单层
  – 平板优先贴菜单右侧，避免被菜单盖住
  – 触控防双触发（pointerup + 吞合成 click）
  – 菜单/抽屉被 ST 重建后入口自动挂回

────────────────
安装（30 秒）
────────────────
1. 酒馆 → 扩展 → 安装扩展
2. 粘贴：
   https://github.com/idx425/st-api-switcher
3. 安装后刷新页面，确认「API 快切」已启用

已安装：扩展面板顶部点「检查更新」，或删除后用同一链接重装
（站点配置一般在本机扩展设置里，重装通常不会丢）

────────────────
使用三步
────────────────
1. 扩展设置里加站点：名称 + URL + Key + 模型（可选分组）→ 保存
2. 插头列表 / 魔法棒 /「使用」按钮 任选一切换
3. 若没连上，到 API 页再点一次 Connect

────────────────
注意
────────────────
• Key 明文存本机扩展设置；导出备份同样含 Key → 只给自己设备用
• 「当前」高亮按 URL 匹配
• 仅作用于：聊天补全 → 自定义（OpenAI 兼容）

反馈 / Issue：https://github.com/idx425/st-api-switcher/issues
```

---

## ② v3.7.2 更新公告

```
🚀 API 快切 v3.7.2 更新

仓库：https://github.com/idx425/st-api-switcher
已装用户：扩展面板顶部点「检查更新」即可

本次重点：修复插头嵌入区收起后搜索栏仍然显示的问题！

✅ 插头嵌入区完整收起
  · 收起 API 快切嵌入区时，搜索栏现在会与站点列表、底部提示一起隐藏
  · 展开后搜索栏正常恢复，不影响搜索词和站点过滤

近期：
· 3.7.1 修复模型搜索弹窗关闭残留、支持回车快捷选中、修复版本回退与更新检测
· 3.7.0 站点搜索过滤（设置面板/快捷面板/插头）、搜索联动、分组搜索、原生同步增强
· 3.6.0 同站多模型折叠快切、原生连接自动同步、保留原生 Key

有问题直接回帖或开 Issue～
```
