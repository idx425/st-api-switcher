# API 快切 (ST API Switcher)

SillyTavern（酒馆）扩展：把 OpenAI 兼容接口的 **URL + API Key + 模型** 绑成站点配置，一键切换并自动重连。

酒馆内置「连接配置文件」不会给每个自定义端点单独存 Key（自定义源共用一个 Key 槽位）。换站时往往要改 URL、重贴 Key、再 Connect。本扩展把这三样做成可分组、可分页的站点列表，点一下整套切换。

- 仓库：https://github.com/idx425/st-api-switcher  
- 当前版本：**v3.5.1**

## 简介（能做什么）

- 🔌 **插头主界面**：在「API Connections / 插头」抽屉内嵌快切列表（手机端主入口）
- 🪄 **魔法棒快捷面板**：扩展菜单里点「API 快切」，弹出站点列表秒切
- 🗂️ **站点卡片**：名称 / URL / Key / 模型，可编辑、删除、导入导出
- 📁 **分组 + 折叠**：站点可归组；组头可收起；当前连接所在组有标记
- 📄 **分层分页（每页 4 条）**：未分组站点、分组列表、组内站点、表单分组芯片各自分页，布局整齐
- 🧩 **获取模型**：从目标接口拉 `/models` 列表点选（优先浏览器直连，失败再走酒馆代理）
- 🔐 **密钥只留一条**：对 `api_key_custom` 串行「先写后清」，同步 `secret_state`，减少「Secret id not found」
- 📱 **三端适配**：手机 / 平板 / 电脑；扩展设置用原生 `inline-drawer`，可收成小横条
- 🎨 **浅色主题可读**：插件自绘区强制暗底亮字，避免白字撞白底

## 安装

1. 打开酒馆 → **扩展** → **安装扩展**
2. 粘贴仓库地址：

```text
https://github.com/idx425/st-api-switcher
```

3. 安装后刷新页面；在扩展列表里确认 **API 快切** 已启用

已安装用户：扩展面板顶部点 **检查更新**，或删除后用同一链接重装（站点配置一般在本机设置里，不会因重装清空）。

## 使用

1. 扩展设置 → 展开 **API 快切** → 填写名称 / URL / Key / 模型（可选分组）→ 保存  
2. 切换入口任选其一：
   - **API Connections（插头）** 抽屉内的列表（手机推荐）
   - 站点卡片上的 **使用**
   - 魔法棒菜单 → **API 快切**
3. 切换后若未连上，到 API 页再点一次 **Connect**

## 更新说明 · v3.5.1

小型稳定性修复：

- **切换等待自定义表单就绪**：先切到 OpenAI 自定义源，再等待 URL 输入框出现，修复部分新版 / 移动端 ST 表单异步重建时误报「未找到输入框」
- **拉模型后清理临时 Key**：当当前连接原本没有 Key 时，代理拉模型写入的临时 Key 会被清空，避免后续 Connect 误用
- **快捷面板 × 更可靠**：补齐纯 click / 键盘关闭路径，避免部分 WebView 或无 PointerEvent 环境下点 × 无反应
- **更新按钮状态修复**：「刷新生效」状态下再次点击会直接刷新页面，而不是重复执行更新
- **分组分页键隔离**：组名与内部分页键重名时不再互相覆盖

### v3.5.0 摘要

全面体检 + 稳定性扫尾（在 3.0.1 关 × 修复之上）：

- **设置清洗更稳**：脏/残缺站点数据自动规范化；仅在确实改过时才写回设置，避免误伤其它字段、也不每次启动空写
- **切换串行 + 忙提示**：连点切换会提示「正在切换」；模型字段始终覆盖（含清空），不再沿用上一站模型
- **Connect 前二次清 Key**：降低可见框被回填后再次追加密钥条目的概率
- **拉模型还原密钥改为 await**：与 `applyProfile` 的 `writeKey` 不再竞态
- **UI 宿主重建自愈**：魔法棒按钮 / 插头嵌入 / 浮层面板在菜单或抽屉被 ST 重建后自动挂回；嵌入区按钮幂等重绑
- **观察器节流 + 不轻易断开**：MutationObserver 合并触发，长期轻量观察，减少入口丢失
- **快捷面板关闭更干净**：模型弹窗打开时不误关快切；× / Esc / 点外只关快切；键盘可开关键棒项
- **版本比较兼容** `v` 前缀与多段号；导出备份 `version` 写入真实插件版本；导入校验 `http(s)` URL

### 近期版本摘要

| 版本 | 内容 |
|------|------|
| **3.5.1** | 小修：切换等待表单、临时 Key 清理、快切 ×、刷新生效按钮、分组页码冲突修复 |
| **3.5.0** | 全面体检：设置清洗、密钥竞态、宿主自愈、切换/导入边界修复 |
| **3.0.1** | 点 × 只关快切面板，不再连带关魔法棒菜单 |
| **3.0.0** | 魔法棒保持打开 + 叉号/外点/Esc 关闭；手机防双触发；切换加锁 |
| **2.3.3** | 魔法棒面板避让菜单 + 稳定定位 |
| **2.3.2** | 浅色主题表单岛全面加深，输入/按钮/芯片可读 |
| **2.3.1** | 切换站点时密钥「先写后清」，修复 Secret id not found |
| **2.3.0** | 站点层 / 组层 / 组内分层分页（每页 4） |

## 注意事项

- 需要 SillyTavern **1.12+**
- Key 以明文存在本机扩展设置里（酒馆通用机制）；导出备份同样含明文 Key，仅建议个人设备使用
- 「当前」高亮按 URL 匹配（Key 无法安全读回比对）
- 仅作用于「聊天补全 → 自定义（OpenAI 兼容）」来源

## License

MIT
